<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.muziuniv_be_notuser.app.notice.NoticeMapper">
     <insert id="insertNotice">
      INSERT INTO notice (notice_id, created_at, update_at, notice_title, notice_content)
         VALUES (#{noticeId}, #{createdAt}, #{updateAt}, #{noticeTitle}, #{noticeContent})

     </insert>

     <select id="selectNotice">
      SELECT notice_id, created_at, update_at, notice_title, notice_content
         FROM notice
         ORDER BY created_at DESC;

    </select>

    <update id="updateNotice">
        UPDATE notice SET

        created_at =  #{createdAt},
        update_at = #{updateAt},
        notice_title =  #{noticeTitle},
        notice_content =  #{noticeContent},
        WHERE notice_id = #{noticeId}

    </update>

    <delete id="deleteNotice">
        delete from notice
        where notice_id = #{noticeId}
    </delete>


</mapper>