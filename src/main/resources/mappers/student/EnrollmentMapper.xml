<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.muziuniv_be_notuser.app.student.enrollment.EnrollmentMapper">

    <!--금학기 내가 수강 신청한 모든 과목들을 조회-->
    <select id = "getMyCurrentEnrollmentsCourses">
        SELECT
        e.status,
        c.course_id,
        c.course_code,
        c.title,
        c.classroom,
        c.type,
        c.grade,
        s.year,
        s.semester,
        c.time,
        c.credit,
        c.max_std,
        c.rem_std,
        c.user_id

        FROM course c
        JOIN enrollment e ON e.course_id = c.course_id
        JOIN semester s ON c.semester_id = s.semester_id
        WHERE e.user_id = #{userId}
        AND s.semester_id = #{semesterId}
        AND e.status = '수강중'
    </select>








</mapper>